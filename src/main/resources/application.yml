spring:
  application:
    name: throttlex
  datasource:
    url: jdbc:mysql://localhost:3306/throttlex?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: false

# Actuator - expose health endpoint
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

# Custom ThrottleX defaults (used when no per-key policy is configured)
throttlex:
  default-capacity: 100
  default-refill-rate: 10
  default-window-seconds: 60
